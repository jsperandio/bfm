name: Hexame
dir:
  app:
    domain:
      model:
      repository:
      service:
    fx:
      module:
        provider:
        server:
        serverless:
        service:
    provider:
    server:
    serverless:
  cmd:
    server:
    serverless:
  configs:
  deployments:
